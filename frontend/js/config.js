// ============================================
// QUINT ESSENTIALS — Frontend Config
// Change these two values to match your setup
// ============================================

const QUINT_CONFIG = Object.freeze({
  // ★ YOUR WHATSAPP NUMBER (international format, no + or spaces)
  WHATSAPP_NUMBER: '254705525427',

  // ★ Backend API base URL
  // Development:  'http://localhost:3000'
  // Production:   'https://api.yourdomain.co.ke'
  API_BASE: 'http://localhost:3000',

  // Admin session key (do not change)
  SESSION_KEY: 'quint_session',
});